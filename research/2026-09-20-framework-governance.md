# 期货框架治理记录 · 2026-09-20

本次按用户最终要求，以 v2.27（git `1e09565`）为基线做尽量内容等价的整理：保留所有有效方法、策略、参数、例外、风控与审计要求；清除修订簿记、被替代的历史状态和重复说明。没有进行新的市场调研，原市场状态日期仍为 2026-09-19。

## 范围与文件结构

保留完整版 `framework/futures_framework.md`、其精简版 `framework/futures_framework_compact.md`，以及原本就存在的 `framework/FUTURES_DATA_PROTOCOL.md`。数据协议恢复为基线内容，不作为新的拆分容器。

撤回上一轮 v3 方案：删除新建的 CONFIG、STRATEGY_A、EXECUTION 三个碎片模块，恢复原策略体系、评分、每周执行审计和影子要求；不再采用“只做 MA/RB A、固定最多两个候选、三条变化、普通研究不评分不审计”的新限制。原框架本来已有的候选计划数量与其他条件不受此撤回影响。

有效规则留在当前框架正文；Git 只保管历史记录，不用于补齐被此次删出的有效规则。完整版与精简版继续是详略关系，精简版保留其完整判据与稳定标识，不改成几步流程的目录。

## 整理原则

- 删除历代版本注记、变更说明、旧周报的重复叙述；最新状态保留其日期、证据限制和仍适用的判断。
- 同一规则集中于权威定义，其他位置引用；例外、解除条件、冻结/休眠及复活条件都属于有效逻辑，不因暂未执行而删除。
- 保留原章节编号与 #1–#31、D8–D15、策略参数/评分、账户及审计定义，使现有数据协议、脚本和模板仍可引用。
- 先判品种/策略/规则适用性，再展开对应内容；这是既有条件化执行的导航，不缩小研究范围或改变交易权限。
- 维护入口仅做必要调整：局部更新主文、原位置覆盖最新状态、报告不粘贴框架全文、以后不累加修改记录。股票轨道保持原流程。
- 治理脚本只做只读结构检查和体积报告，不再用任意硬上限强制删内容，也不再从主文几段摘要自动生成精简版。

## 原有定义缺口

原 v2.27 有少量更早版本引用未在本文件完整附载，包括事件表“沿 v2.8”、国债曲线原参数、D13 部分归档文本和 F 的“三重验证”详项。最早的仓库 v2.9 也已有这些引用；本次没有编造缺失定义，也不把 G 的验证条件直接当作 F 的定义。这些是原有方法问题，和本次删修订记录分开记录。

## 验证

尺寸按 UTF-8 字节比较；精简版将部分长段分行，行数增加不代表内容增加。最终尺寸见下表。

| 文件 | 原字节数 | 整理后字节数 | 减少 |
|---|---:|---:|---:|
| 完整版 | 317,169 | 214,869 | 32.25% |
| 精简版 | 112,703 | 103,311 | 8.33% |
| 既有数据协议 | 21,558 | 21,558 | 0% |

- 精简版原有 277 个稳定规则标识全部保留，无缺失、改名或新增。逐规则数字差异已人工复核：差异来自重复阈值合并、旧版本/日期说明移除、数值写法与表头整理，未发现有效参数遗漏。
- 完整版保留 Step0–6、#1–#31、评分维度、全部策略定义、冻结/复活条件及 schema 3 审计要求。关键参数表、A/B 定义和风险计算块做了规范化对照；仓位公式去除注释/空行后与基线一致。
- 另行抽查事件表、D13、双轴持仓动作、止盈、D/E/C 状态清理；未发现本次新增的有效阈值、触发或例外遗漏。旧单次事件预案只在已有后续结果覆盖时删除；参考级专业读数的旧触发按基线 0D 的既有覆盖关系清理。
- 数据协议、审计模板、季节计划输入文档、行情脚本、价格证据模块、风险计算器、审计校验器与季节计划工具均与基线逐字节一致。
- `python3 scripts/futures_framework_governance.py --check`：通过规则标识、相对链接与历史修订标记检查；脚本只读，不生成或截断框架，不设压缩比例。
- `python3 -m unittest discover -s tests -p 'test_futures_framework_governance.py' -v`：12 项通过；`git diff --check` 通过。

既有全量测试在基线有 1 个 error 与 2 个子测试失败，位于 `test_price_evidence.py` 的隔离加载/价格证据用例；本次不改该测试及相关行情算法，不宣称全量测试通过。没有重新采集行情或回测，结构检查与数值对照也不能单独证明全部自然语言规则的语义等价。
