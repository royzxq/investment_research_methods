# 股票框架治理记录 · 2026-09-21

沿用用户已确认的治理边界：完整版与精简版是同一框架的详略表达，尽量在有效内容等价的基础上整理；不缩小研究范围，不改变分析环节、数值参数、合取条件、例外、冻结与解除规则，也不以目标压缩率倒推删除内容。

基线为本次整理前的两份股票文件（git `1e09565`）。市场状态仍截至 2026-09-19；基线已有的 2026-09-20 三项合取条件澄清继续有效。此次没有重新采集或核验市场信息，没有把整理日期当成行情日期。

## 文件与内容

保留 `framework/investment_framework.md` 与 `framework/investment_framework_compact.md`，没有新增方法模块或股票数据协议。完整规则仍在这两份当前文件内，历史由 git 与已有日期研究报告保存。

完整版主要清理全局规则、机制权重与机制状态中的历周演变，保留最新状态及仍有效的重建、回退、升降档条件；输出模板引用权威日历和状态，避免重复粘贴整段市场记录。精简版已采用 R/D 编号，本次只做必要清理与长段分行，不强行重构。

## 既有判据的指代修正

基线完整版的“隐含降息前置否决检查”已明确：含 CPI>4% 的硬阈值为独立、更严的合取条件，当期 CPI 分支未命中；压测冻结采用另一组三项合取，并保留“盈利能够对冲估值压缩”的判断边界。

本次据此修正精简版 R3 中把 R2 也写成引用同一判据的指代，及完整版相关单条件简写，保留 R2/R3 各自条件与动作。状态标题明确为“转加息分支正式落地”，不把一个分支落地写成完整硬阈值命中。全面执行的参数档位继续保持原定义。

## 后续维护

- adaption 只局部更新受影响内容，报告给完整框架链接与必要片段，不再粘贴整个框架。
- 当前状态原位替换；历次修改标记、旧状态和修改过程不累加进框架。变化检测需要历史时，读取已有日期研究/变化/适配报告及必要 git 变更。
- 共享 `framework-condense` 统一股票与期货的保真维护原则，取消强制压缩比例和每次全量重写；局部同步后仍对照全文核验。
- 保留稳定规则编号、章节映射与来源；不要求完整版和精简版章节结构相同。compact 可作实际标的研究导航，方法修订、参数核对及歧义裁决仍回到完整版。
- 股票各阶段原输入变量、比较字段、首次运行逻辑、分析要求及完整流水线发布边界保留。本次只保存本地整理结果，未运行周度调研或发布流程。

## 验证

| 文件 | 整理前 UTF-8 字节 | 整理后 UTF-8 字节 | 减少 |
|---|---:|---:|---:|
| 完整版 | 88,994 | 74,891 | 15.85% |
| 精简版 | 54,041 | 52,526 | 2.80% |

精简版原本已有较集中的规则系统，本次不追求更大压缩。为便于阅读，部分长段拆成多行，因此行数增加。

- 两份框架保留 `COMPANY_NAME`、`TICKER_OR_CODE`、`VALUATION_DATE` 三个模板变量；股票阶段指令的原输入变量集合也保持一致。
- 精简版的 R1–R20、D1–D8 共 28 个定义及其引用完整，无改名、重复或悬空引用；10 闸表与 0–8 输出契约保留。全部 69 处行内代码片段、Precheck 表及输出契约与基线逐字一致。
- 精简版数据契约、Precheck、机制、Gate、估值、Terminal、Price Map 与输出各整节在忽略分行后与基线一致。固定 R 规则排除来源/编号/修订日期后的数字多重集一致；动态配置删去的数字来自重复日期或已被最新状态取代的旧读数。
- 完整版完成 34 项结构与重点内容对照：包含数据契约、硬失败及判定规则、机制鉴别清单、Gate 顺序、估值验证链、Terminal 参数、仓位公式、T1/T2、熔断及关键例外。最新负反馈修复状态的证据、回退动作与新承接/存量区别保留。
- 独立复核完整版的负反馈重建/回退、周期需求三项合取、油价与贵金属升降档，以及 §4.2 重估义务，未发现本次新增的有效条件或例外遗漏；这是重点抽查结论，不代表全部语义已形式化证明。
- 只读结构检查通过：本地链接、模板变量、公式、R/D 引用、Precheck 行、输出章节、Markdown 围栏及修订标记清理。结构检查不运行行情或估值。
- 独立情形抽检：R1 仅部分条件命中不能机械冻结；R3 的盈利对冲条件不成立时不触发该合取冻结，R18 参数仍从严；规划型标的缺第二环证据时仍只能观察。均未把局部规则通过当成整体买入许可。
- 5 个改动技能通过 `skill-creator` 的 `quick_validate.py`；`git diff --check` 通过。共享维护规则调整后，既有期货治理检查仍通过，未改动期货两份框架或数据协议。

规则编号、数值与公式对照只能辅助检查，不能单独证明所有自然语言规则语义等价。本次没有增加投资方法、市场结论或取数能力；原框架未量化的参数档位等缺口不通过精简自行补数。

原有未量化项包括 `d_base` 具体区间、单票/温度仓位上限、冷中热切点、Chip FREEZE 与治理红线明细、折扣“一档”的幅度；P2 公式的 `step_down` 与说明中的 `step` 名称也保持原式。本次没有将这些边界擅自补为新参数。
